version: '3'

services:
  woeden_agent:
    build:
      context: .
    network_mode: host
    environment:
      - HOST=ssl://mqtt.woeden.com
    volumes:
      - ~/woeden:/woeden

volumes:
  woeden: